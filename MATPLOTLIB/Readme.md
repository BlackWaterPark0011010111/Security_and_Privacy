Matplotlib Pyplot
Matplotlib Plotting
Matplotlib Markers
Matplotlib Line
Matplotlib Labels
Matplotlib Grid
Matplotlib Subplot
Matplotlib Scatter
Matplotlib Bars
Matplotlib Histograms
Matplotlib Pie Charts  


PYPLOT:
 изначально пакет pyplot- это и есть основная лошадка,которая и запускает все в matplotlib.там те функции которые отвечают за отображение всех графиков.
обращаемся к ветке pyplot->
matplotlib.pyplot -> import matplotlib.pyplot as plt
```
указываем координаты на оси[1,4,8,6,9] 
->plt.plot([1,4,8,6,9])  
```  
и     
plt.show()-чтобы увидеть график
show()->указывает программе не закрывать окно.если ее не ввести,окно графика откроется но настолько же быстро и закроется.потому что команда show,фиксирует отображение графика на экране.
мы также можем использовать библиотеку numpy,и код будет выглядеть проще
например без numpy:
```
import matplotlib.pyplot as plt
plt.plot([1,4,8,6,9])
plt.show()
```
и с numpy:

import numpy as np
и тут мы сразу устанавливаем значения по оси 'Y'->
```
y=np.array([1,4,8,6,9]) --> точки по координате 'Y'
plt.plot(y)
plt.show()
```
когда мы в plot передавали  массив данных без использования numpy,то там сначала происходила конвертация  с использованием библиотеки numpy, и только потом эти данные отображались,а можно сразу прописывать импорт numpy и писать координаты для оси через numpy. И вообще почти все функции пакета matplotlib работают через библиотеку numpy.
когда мы передаем значения по оси координат 'Y', то x автоматически приобретает значения от 0 и до ......

НАПРИМЕР:

Х  0  1  2  3  4 --выставляются автоматически,даже если мы их не указали
Y  1  2  9  5  4
И ЕСЛИ УКАЗЫВАЕМ:
```
x=np.array([1,4,5,8,7])
y=np.array([1,4,8,6,9])
plt.plot(x,y)-- и если мы в plot, перелаем 2 аргумента x и y,то они передаются только так,через запятую.
plt.show()
```

ЛИБО:
мы прописываем значения для Y от 0 до 4:

```y=np.arange(0,5,1)  -->[0, 1, 2, 3, 4]  с помощью arange  с лева на право,движение вперед,вверх
np.arange(5, 0, -1) ---с права на лево,движение назад,вниз 
шаг 1
```
``` 
x=np.array([a*a for a in y]) --> [0,1,4,9,16]
```

для оси x- он будет меняться от 0 и до 16,то есть мы значения Y возводим в квадрат и будет получен список для X.

Чтобы увидеть сетку --- grid ---> plt.grid()

чтобы увидеть 2 графика,можно добавить 2 коллекции:
```
x1=[1,4,5,8,7]
y1=[a*a for a in y]
```
И
```
x2=[0,1,2,3]
y2=[i+1 for i in y2]
plt.plot(x1,y1,x2,y2)
plt.show()
```

В matplotlib-нет согласованности данных и все будет показано так как согласовано.

plt.plot(x1,y1,'--') ---> 

'--' -означает что график будем видеть штриховой,сам тип линии при отображение графика.

'-'   ---> непрерывная линия(по умолчанию)  plt.plot(x1,y1,'-')
'--'  --->  штриховая линия  plt.plot(x1,y1,'--')
'-.'  --->  штрихпунктирная линия   plt.plot(x1,y1,'-.')
':'   --->  пунктирная линия    plt.plot(x1,y1,':')
'None' или '' ---> без рисования линия (вообще ничего не увидим)plt.plot(x1,y1,None(''))
НО!
у нас есть график и он уже в 2D. и мы можем управлять этим отображением
НАПРИМЕР:
lines=plt.plot(x,y,':')
plt.setp(lines,linestyle=':')
 есть специальная функция которая позволяет установить специальные свойства
"SETP"  и нам нужно прописать в plt так же эту функцию со специальным именованным параметром "linestyle". и так мы можем менять внешний вид линий
и если мы передаем в LINES графики  x1,y1,x2,y2,x3,y3 --- то все они будут такими которыми мы задали в linestyle.

ТАКЖЕ мы можем менять их цвет------------

🔵	'b'	blue	синий
🟢	'g'	green	зеленый
🔴	'r'	red	красный
🟡	'y'	yellow	желтый
⚫	'k'	black	черный
⚪	'w'	white	белый
🎨	'c'	cyan	голубой
🟣	'm'	magenta	пурпурный

plt.plot(x, y, 'ro')     ----> красные кружки
plt.plot(x, y, 'b--',x2, y2,'k:')    ----> синяя штриховая линия
plt.plot(x, y, 'g^-')    ----> зеленые треугольники со сплошной линией
ЧЕРЕЗ НАЗВАНИЯ:
plt.plot(x, y, color='red')
plt.plot(x, y, color='steelblue')
                 HEX код
🔵	'blue'	    #0000FF
🟢	'green'	    #008000
🔴	'red'	    #FF0000
🟡	'yellow'	#FFFF00
🟠	'orange'	#FFA500
🟣	'purple'	#800080
🎨	'cyan'	    #00FFFF
🌸	'pink'	    #FFC0CB
🟤	'brown'	    #A52A2A
🔶	'coral'	    #FF7F50
�️	'gold'	     #FFD700
🌿	'lime'	    #00FF00
🔷	'navy'	    #000080
🫐	'teal'	    #008080
🍇	'violet'	#EE82EE

plt.plot(x, y, color='#FF5733')
plt.plot(x, y, color='#33FF57')

RGB-кортеж:

plt.plot(x, y, color=(0.1, 0.2, 0.5))  -----> значения от 0 до 1
plt.plot(x, y, color=(255, 0, 0))  



МАРКЕРЫ
есть именованный параметр ---> marker=''
lines=plt.plot(x,y 'r--o',marker='s',markerfacecolor='w')
lines=plt.plot(оси,цвет и фигуры на штрихе , цвет точки x и y,толщина линии(linewidth=4) )
●	'.'	точка (маленькая)
◆	','	пиксель (очень маленький)
◯	'o'	круг
+	'+'	плюс
✕	'x'	крестик
△	'^'	треугольник вверх
▽	'v'	треугольник вниз
◁	'<'	треугольник влево
▷	'>'	треугольник вправо
☆	'*'	звезда
□	's'	квадрат
◇	'D'	ромб (diamond)
▷◁	'd'	тонкий ромб
▼	'p'	пятиугольник (pentagon)
⬡	'h'	шестиугольник (hexagon)
✶	'H'	шестиугольник (другой тип)
─	'_'	горизонтальная линия
│	'|'	вертикальная линия
◉	'None' или ' '	без маркера
◎	'none'	без маркера

Маркеры из LaTeX
$\star$	'*'	звезда
$\dagger$	'1'	кинжал
$\ddagger$	'2'	двойной кинжал
$\diamond$	'3'	ромб
$\triangledown$	'4'	треугольник вниз

Числовые коды маркеров
'0'	треугольник (tickleft)
'1'	треугольник (tickright)
'2'	треугольник (tickup)
'3'	треугольник (tickdown)
'4'	карет (caret)
'8'	восьмиугольник

plt.plot(x, y, 'ro-')   ----> красные кружки со сплошной линией
plt.show()

plt.plot(x, y, 'b^--')  ----> синие треугольники с штриховой линией
plt.show()

plt.plot(x, y, 'gs:')   ----> зеленые квадраты с пунктирной линией
plt.show()

Заливка областей графика

fill_between(x,y1,y2=0,where=None,interpolate=False,step=None,*,data=None,**kwargs)
(первая кривая относительно которой делается заливка, вторая кривая,)

```
x=np.arange(-2*np.pi,2*np.pi,0.1)
y=np.cos(x)
plt.plot(x,y)
plt.fill_between(x,y,0.5)  ИЛИ
plt.fill_between(x,y,where(y < 0),color='r', alpha=0.5)
plt.fill_between(x,y,where(y > 0),color='k', alpha=0.5)
plt.grid()
plt.show()
```

Отображение нескольких графиков в одном окне

Благодаря функции    SUBPLOT
subplot(nrows,ncols,index)

nrows,ncols ==число строк и столбцов
index== индекс текущих координатных осей
import numpy as np
import matplotlib.pyplot as plt
plt.subplot(1,3,1) <--1 
plt.plot(np.random.random(10))
plt.subplot(1,3,2) <--2 
plt.plot(np.random.random(10))
plt.subplot(1,3,3) <--3
plt.plot(np.random.random(10))
plt.grid()   <---сетка
plt.show

3 раза вызывается subplot, где создается ось с координатами,и в каждых осях
отображается график со случайными значениями
но мы тут прописываем grid только после самой последней оси и применятся она будет только к последней,чтобы сетка была видна в каждом графике:
мы помещаем координаты в переменную и потом дописываем к ней grid()
```
ax1=plt.subplot(1,3,1) <--1 
plt.plot(np.random.random(10))

ax2=plt.subplot(1,3,2) <--2 
plt.plot(np.random.random(10))

ax3=plt.subplot(1,3,3) <--3
plt.plot(np.random.random(10))

ax1.grid()
ax2.grid()
ax3.grid()  
plt.show
```


ТОЧКИ
Нанесение и оформление отдельных точек функцией scatter()
Для нанесения на диаграмму отдельной точки используется функция scatter(). 
мы передаем координаты как просто одной точки так и можно нескольких: scatter() координаты (x, y) нужной точки, и функция нанесет эти 
значения на диаграмму:
fig,ax=plt.subplots()
ax.scatter(2,4) <--- одна точка на пересечении
ax.scatter(2,4бы=200)<---можно добавить размер шрифта


и серию точек:::
x_values = [1, 2, 3, 4, 5]
y_values = [1, 4, 9, 16, 25]
fig, ax = plt.subplots()
ax.scatter(x_values, y_values, s=100)


subplot(nrows,ncols,index)
subplot(1,3,1)
subplot(1,3,2)
subplot(1,3,3)
nrows,ncols-число строк и столбцов
index= индекс существующих координатных осей









f,ax=plt.subplots(2,2)#по 2 графика в двух строках
plt.show()

╔═════════════╦═════════════╗
║             ║             ║
║    ax[0,0]  ║    ax[0,1]  ║
║             ║             ║
╠═════════════╬═════════════╣
║             ║             ║
║    ax[1,0]  ║    ax[1,1]  ║
║             ║             ║
╚═════════════╩═════════════╝


РАЗНИЦА МЕЖДУ AX1.plot() И PLT.plot() 


ax.plot()=это объектно ориентированный подход, работа с конкретными объектами которым уже дано название. более чистый и понятный код. больше контроля над каждым графиком. лучше для сложных проектов
fig, ax = plt.subplots(2, 2)  # ax - это массивы,сами формы где находится график 2x2
fig-это само окно которое всплывает при запуске кода

# Работаем с каждым графиком напрямую через его приобретенное название
# ax[0, 0].plot([1, 2, 3], [1, 2, 1])
# ax[0, 0].set_title("График 1")
 
# ax[0, 1].plot([1, 2, 3], [1, 4, 9])  
# ax[0, 1].set_title("График 2")
 
# ax[1, 0].plot([1, 2, 3], [2, 1, 2])
# ax[1, 0].set_title("График 3")
 
# ax[1, 1].plot([1, 2, 3], [3, 2, 3])
# ax[1, 1].set_title("График 4")
 
# plt.tight_layout()
# plt.show()

plt.plot() ===State-based подход
просто обычная текущая работа с данным активным графиком
переключаемся между графиками  с помощью plt.subplot(). 
простой но менее гибкий

import matplotlib.pyplot as plt

# Создаем сетку 2x2
# plt.subplot(2, 2, 1)  # Переключаемся на 1-й график
# plt.plot([1, 2, 3], [1, 2, 1])
# plt.title("График 1")
# 
# plt.subplot(2, 2, 2)  # Переключаемся на 2-й график  
# plt.plot([1, 2, 3], [1, 4, 9])
# plt.title("График 2")
# 
# plt.subplot(2, 2, 3)  # Переключаемся на 3-й график
# plt.plot([1, 2, 3], [2, 1, 2])
# plt.title("График 3")
# 
# plt.subplot(2, 2, 4)  # Переключаемся на 4-й график
# plt.plot([1, 2, 3], [3, 2, 3])
# plt.title("График 4")
# 
# plt.tight_layout()
# plt.show()


РАЗНИЦА МЕЖДУ add_axes([0.0,0,1.0,1.0])   и    add_subplot(1,3,1)
add_axes == создает рисунок на все открывшееся окно
add_subplot=== создает график с параметрами осей и графиком в окне 


граничные значения благодаря которым можно задавать для оси X и Y

Управление положением меток на координатах оси x и y
для этого в пакете matplotlib есть две функции:
set_major_locator()
                    И
set_minor_locator()
В качестве аргумента в этих функциях выступает класс matplotlib.ticker.Locator

from matplotlib.ticker import NullLocator
...................
.................
...............

lc=NullLocator()
ax.grid()
ax.xaxis.set_major_locator(lc)
                                ИЛИ чтобы не создавать обьект класса
ax.xaxis.set_major_locator(NullLocator())                               
xaxis= объект оси X.
set_major_locator(lc)=мы вызываем эту функцию для объекта xaxis  и в качестве аргумента выступает экземпляр класса NullLocator -lc
NullLocator- используется для сокрытия меток по выбранной оси(X xaxis или Y yaxis) то есть будут горизонтальные линии
Если ввести 

ax.yaxis.set_major_locator(lc)
                                ИЛИ чтобы не создавать обьект класса
ax.yaxis.set_major_locator(NullLocator())                               


то на графике будут вертикальные линии






MultipleLocator класс
ax.xaxis.set_major_locator(MultipleLocator(base=1))
ax.grid()
plt.show()

MultipleLocator = он позволяет задавать шаг между рисочками на оси координат 
и для этого используется параметр base к которому приравнивается значение точнее сколько отрезов мы хотим видеть на оси
(MultipleLocator(base=1)) === значит цифры будут выставляться на 1-цу больше,то есть 1,2,3,.......